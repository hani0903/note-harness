# DO — 해야 하는 것

- 톤 차이로 레이어를 표현한다
- 여백으로 정보를 그룹화한다
- 콘텐츠가 가장 먼저 보이게 한다
- subtle한 hover/interaction을 사용한다
- UI보다 읽기 경험을 우선한다
- 경계는 배경 색상 시프트로만 표현한다
- 리스트 항목 간격은 `spacing.4`로 구분한다
- Focus 상태에서만 선을 명확하게 드러낸다
- Primary 버튼에는 `tertiary → tertiary_container` gradient를 사용한다
- 플로팅 요소(모달, 드롭다운)에만 Glassmorphism을 적용한다
